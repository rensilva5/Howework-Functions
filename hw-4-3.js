// Create a function that converts a string to all uppercase letters and console.log the result
// (hint: string methods):

// let's create a const w/ "WHAT a TIME to BE ALIVE!"

let convertToUpper = "WHAT a TIME to BE ALIVE!"
function convert (str) {
    return str.toUpperCase()  // 
   
}
//console.log(UpperCase())
//phrase.toUpperCase()
const toUppercase = convert(convertToUpper)
console.log(toUppercase)
//  -- or we could also console.log=
console.log(convert(convertToUpper))